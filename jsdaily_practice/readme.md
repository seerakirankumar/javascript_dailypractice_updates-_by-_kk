Day 1 – Grade Calculator

Used alert() and prompt() for user interaction

Implemented if-else if conditions

Built a simple Marks Portal that:

Takes marks as input

Displays Grade (A, B, C, D, F)

✅ Concepts Practiced:

Conditional statements

Logical operators (&&)

Comparison operators

🗓️ Day 3 – Array Average & 10% Offer
📌 Finding Average of an Array

Used for loop

Calculated total sum

Found average using formula:

Average = Total Sum / Number of Elements

📌 Providing 10% Discount

Calculated 10% of each value

Subtracted discount from original value

Displayed discounted prices

✅ Concepts Practiced:

Arrays

Looping through arrays

Arithmetic operations

🗓️ Day 4 – Array Practice (Revision)

Repeated average calculation logic

Practiced discount logic again

Improved understanding of loops and variables

✅ Focus:

Strengthening fundamentals

Code repetition for clarity

🗓️ Day 5 – Vowel Counter Function

Created a function that:

Takes a string input

Counts number of vowels (a, e, i, o, u)

Uses for loop and conditional checks

✅ Concepts Practiced:

Functions

String indexing

Conditional logic

🗓️ Day 6 – Array Methods Practice
📌 1. Square of Numbers

Used forEach() method

Found square of each number in array

📌 2. Filter Students with Marks > 90

Used filter() method

Returned only marks greater than 90

📌 3. Reduce Practice (Sum & Product)

Created an array from user input

Used reduce() to:

Calculate total sum

Calculate multiplication of numbers

✅ Concepts Practiced:

forEach()

filter()

reduce()

Arrow functions

Higher-order functions

🎯 What I Learned

How to work with arrays efficiently

How to use different loop types

Understanding of higher-order functions

Writing reusable functions

Improving logical thinking

>>DAY [7]

DOM in JavaScript
🔹 What is DOM?

DOM (Document Object Model) is a programming interface that allows JavaScript to interact with and manipulate HTML documents.

When a web page loads in the browser, the browser converts the HTML document into a structured tree of objects. This structure is called the DOM Tree.

JavaScript uses the DOM to access and modify webpage content dynamically.

🌳 How DOM Works

The browser reads HTML.

It converts HTML elements into objects (nodes).

These objects are organized in a tree-like structure.

JavaScript can access and modify these objects.

Example DOM Structure:

Document
 └── html
      └── body
           ├── h1
           └── p

Each HTML element becomes a node that JavaScript can control.

🔹 Why DOM is Important?

The DOM allows developers to:

Change text content

Modify styles (CSS)

Add new elements

Remove elements

Handle user events (click, input, etc.)

Create dynamic and interactive web applications

Without the DOM, JavaScript cannot update or control HTML content.

🔹 Common DOM Methods

Some frequently used DOM methods include:

document.getElementById()

document.querySelector()

document.getElementsByClassName()

addEventListener()

These methods help in selecting and manipulating elements.

🔹 DOM Manipulation

DOM manipulation refers to changing the structure, style, or content of a webpage using JavaScript.

Examples:

Updating text inside an element

Changing background color

Creating a new element and appending it to the page

Removing an existing element

🔹 Simple Definition

The DOM is the bridge between HTML and JavaScript that allows dynamic changes to a webpage.
